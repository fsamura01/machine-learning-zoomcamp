[project]
name = "smart-fashion-classifier-capstone-project"
version = "0.1.0"
description = "Fashion classifier gateway service"
readme = "README.md"
requires-python = ">=3.11,<3.13"

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"