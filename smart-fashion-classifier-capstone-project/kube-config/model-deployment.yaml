apiVersion: apps/v1
kind: Deployment
metadata:
  name: tf-serving-model
  labels:
    app: model
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tf-serving-model
  template:
    metadata:
      labels:
        app: tf-serving-model
    spec:
      containers:
      - name: tf-serving-model
        image: clothing-model:v1
        ports:
        - containerPort: 8500
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
        